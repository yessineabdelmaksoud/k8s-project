# =============================================================================
# Handlers for docker role
# =============================================================================
---

- name: restart docker
  ansible.builtin.systemd:
    name: docker
    state: restarted

- name: restart containerd
  ansible.builtin.systemd:
    name: containerd
    state: restarted
